import 'package:flutter/material.dart';

class AppColors {
  // Primary colors matching your logo's 3D hexagonal design
  static const Color primaryBlue = Color(0xFF0066FF); // Bright blue from your logo
  static const Color accentTeal = Color(0xFF00D4FF); // Neon cyan from your logo
  static const Color neonBlue = Color(0xFF4D9FFF); // Light blue accent
  static const Color deepBlue = Color(0xFF003D99); // Darker blue for depth
  
  // Dark theme colors (matching your logo's dark background)
  static const Color darkBackground = Color(0xFF0A0E1A); // Very dark blue-black
  static const Color darkSurface = Color(0xFF1A1F2E); // Dark blue-grey
  static const Color darkCard = Color(0xFF252B3D); // Slightly lighter for cards
  
  // Light theme colors
  static const Color lightBackground = Color(0xFFF8FAFC);
  static const Color lightSurface = Color(0xFFFFFFFF);
  static const Color lightCard = Color(0xFFF1F5F9);
  
  // Text colors
  static const Color textPrimary = Color(0xFFE8F0FF); // Light blue-white
  static const Color textSecondary = Color(0xFF94A3B8);
  static const Color textDark = Color(0xFF1E293B);
  
  // Status colors
  static const Color success = Color(0xFF00FF88); // Neon green
  static const Color error = Color(0xFFFF4757); // Bright red
  static const Color warning = Color(0xFFFFB800); // Bright orange
}
